/**
 * 构建生产环境的配置文件名称
 *
 * The name of the configuration file to build the production environment
 */
export const GLOB_CONFIG_FILE_NAME = 'app.config.js'

/**
 * 构建打包的目录名称，相对于项目根目录
 *
 * The directory name of the build package, relative to the project root directory
 */
export const OUTPUT_DIR = 'dist'

/**
 * 构建的开发环境版本
 *
 * Build the development environment version
 */
export const BUILD_VERSION = '0.5.4'
